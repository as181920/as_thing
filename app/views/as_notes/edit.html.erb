<div class="header_menu">
  <%= link_to 'Cancel', @as_note %> &nbsp;
  <%= link_to 'Set_Note_Columns', as_note_as_labels_path(@as_note) %> &nbsp;
  <%= link_to 'Destroy_Note', @as_note, :confirm => 'Are you sure to delete this notes and all releated label/data?', :method => :delete %> &nbsp;
  <%= link_to "View_Note_Records", as_note_as_values_path(@as_note) %> 
</div>

<b><%= link_to @as_note.name, as_note_as_values_path(@as_note) %>: Set_Note_Information</b>

<%= render 'form' %>

</BR>
</BR>

<b><%= link_to @as_note.name, as_note_as_values_path(@as_note) %>: Set_Note_Position =></b>
<%= select_tag(:s_position,options_for_select(@position_sorted,@position)) %>

</BR>
</BR>

